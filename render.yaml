services:
  - type: web
    name: threshold-manor-signal
    runtime: node
    plan: free
    rootDir: signal-server
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
