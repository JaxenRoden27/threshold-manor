name: Deploy signal server

on:
  workflow_call:
    outputs:
      signal_host:
        description: WebSocket signal host (no protocol)
        value: ${{ jobs.deploy.outputs.signal_host }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    outputs:
      signal_host: ${{ steps.host.outputs.signal_host }}
    steps:
      - uses: actions/checkout@v4
      - name: Resolve signal host
        id: host
        run: |
          CANDIDATES=(
            "${{ vars.SIGNAL_HOST }}"
            "threshold-manor-signal.onrender.com"
          )
          for host in "${CANDIDATES[@]}"; do
            [ -z "$host" ] && continue
            if curl -fsS "https://${host}/health" | grep -q ok; then
              echo "signal_host=${host}" >> "$GITHUB_OUTPUT"
              echo "Using signal host: ${host}"
              exit 0
            fi
            echo "Signal host not ready: ${host}"
          done
          echo "::error::No healthy signal server found. Deploy Render via render.yaml or set the SIGNAL_HOST repo variable."
          exit 1
      - name: Smoke test WebSocket signaling
        run: |
          node --input-type=module -e "
          const host = process.env.SIGNAL_HOST;
          const code = 'SMOK';
          const clientId = 'smoke-host';
          const url = 'wss://' + host + '/' + code;
          const ws = new WebSocket(url);
          const timeout = setTimeout(() => {
            console.error('WebSocket smoke test timed out');
            process.exit(1);
          }, 30000);
          ws.addEventListener('open', () => {
            ws.send(JSON.stringify({
              type: 'host-create',
              clientId,
              roomCode: code,
              maxPlayers: 4,
            }));
          });
          ws.addEventListener('message', (event) => {
            const msg = JSON.parse(String(event.data));
            if (msg.type === 'session-ready' && msg.isHost) {
              clearTimeout(timeout);
              ws.close();
              console.log('WebSocket smoke test passed');
              process.exit(0);
            }
          });
          ws.addEventListener('error', () => {
            clearTimeout(timeout);
            console.error('WebSocket smoke test failed');
            process.exit(1);
          });
          "
        env:
          SIGNAL_HOST: ${{ steps.host.outputs.signal_host }}
